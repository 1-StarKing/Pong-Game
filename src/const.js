const PLAYER_WIDTH = 0.1; // Width of the player
const PLAYER_HEIGHT = 1; // Height of the player (you may need to adjust this based on your Player component)
const PLANE_WIDTH = 10;
const PLANE_HEIGHT = 7; // Height of the plane (from your planeGeometry)

module.exports = { PLAYER_HEIGHT, PLAYER_WIDTH, PLANE_HEIGHT, PLANE_WIDTH };
